<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RedRainstyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/RedRainActivity_style</item>
    </style>

    <style name="RedRainActivity_style" parent="@android:style/Animation">
        <item name="android:activityOpenEnterAnimation">@anim/anim_rain_in</item>
        <item name="android:activityOpenExitAnimation">@anim/anim_rain_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/anim_rain_in</item>
        <item name="android:activityCloseExitAnimation">@anim/anim_rain_out</item>
    </style>

</resources>
